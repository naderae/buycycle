
<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>




<h1>Signup!</h1>

<%= form_for :user, url: '/users' do |f| %>
  

  Name: <%= f.text_field :username %>

  Email: <%= f.text_field :email %>

  role: <%= f.select :role, ['Provide Service', 'Request Service'], :prompt => 'Select One' %>

  Service Type: <%= f.select :service, ['Tutoring', 'Home Improvement', 'Personal Training'] %>

  Password: <%= f.password_field :password %>

  Password Confirmation: <%= f.password_field :password_confirmation %>

  <%= f.submit "Submit" %>

<% end %>
